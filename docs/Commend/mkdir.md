# mkdir命令

## 总述
```bash
mkdir [-p] linux路径
```

* `mkdir`创建目录

## [-p]

* 可选,适用于创建连续多层级的目录
* `mkdir -p ~/god/cat`创建god目录及其下的cat目录

## linux路径

* 必填,表示要创建的文件夹的路径,相对路径或绝对路径均可

## 拓展

* `mkdir`来自Make Directory
* `-p`好像来自`-parents`

